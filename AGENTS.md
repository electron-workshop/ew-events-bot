# EW Events Bot

Telegram bot that turns an event link into a Google Calendar entry, so people can submit events without needing a Google account.

- `/docs` — project context, architecture decisions, and open questions. Read this before making design changes.
- `/src` — the actual code.

See `/docs/overview.md` for the full picture before writing code.
